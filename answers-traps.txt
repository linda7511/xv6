1.a0-a7包含函数的参数。由上图可看出寄存器a2保存13。
2.查看asm文件，发现没有对f,g的调用，g被内联f函数（见下图），f函数被内联到main函数
3.在0x628
4.ra保存的是函数调用后返回的地址，由上图可以看出ra的值是0x38, 对应的是0x34的下一条指令。当printf执行完成后，会让pc=寄存器ra的值，然后程序会执行main函数中的0x38这行的代码.
5.
1)输出是He110 World。57616，十六进制表示为 e110。小端：低地址在前，变量i会以如下方式存储在内存中:0x72 ('r') | 0x6c ('l') | 0x64 ('d') | 0x00。所以输出是He110 World。
2)如果在大端上，为了产生相同的输出，i应该设置为0x726c6400
3) 而对于int的数据，则无论大小端输出的值都一样,所以不需要更改57616。
6.根据 C 语言的规则，当 printf 函数的参数数量少于格式控制符的数量时，它会读取内存中的随机值，并且结果是不确定的，可能输出任意的值。
